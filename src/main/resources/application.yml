spring:
  profiles:
    active: local

  jpa:
    properties:
      hibernate:
        use_sql_comments: true

  redis:
    session:
     host: localhost
     port: 6379

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


jasypt:
  encryptor:
    bean: jasyptStringEncryptor

